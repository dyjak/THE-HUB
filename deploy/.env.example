# Public URL you will use inside Tailscale (MagicDNS name is easiest)
# If you use `tailscale serve --http=80 ...`, you typically don't need a port here.
# Example: HUB_PUBLIC_URL=http://thehub-vps
HUB_PUBLIC_URL=http://thehub-vps

# (Opcjonalnie) Tryb publiczny (bez Tailscale): domena + HTTPS + Basic Auth
# HUB_DOMAIN=twoja-domena.pl
# BASIC_AUTH_USER=login
# BASIC_AUTH_HASH=$$2a$$14$$... (wygeneruj przez: docker run --rm caddy:2-alpine caddy hash-password --plaintext 'twojehaslo')
# UWAGA: w pliku `.env` dla docker compose znak `$` trzeba uciekać jako `$$`.

# Required secrets
AUTH_SECRET_KEY=replace-with-long-random
NEXTAUTH_SECRET=replace-with-long-random

# Optional AI providers
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
# OPENROUTER_API_KEY=
